# Vite + Node.js Backend Setup

A minimal starter showing how to connect a Vite frontend with a Node.js backend.

## Run it

```bash
cd backend && npm install && npm run start
cd frontend && npm install && npm run dev
```

## Stack

Vite (frontend) + Node.js/Express (backend), CORS-enabled for local dev.
